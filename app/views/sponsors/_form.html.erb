<div class="form-container">
  <%= form_with(model: sponsor, local: true) do |form| %>
    <% if sponsor.errors.any? %>
      <div class="error-messages">
        <h4><%= pluralize(sponsor.errors.count, "error") %> prohibited this sponsor from being saved:</h4>
        <ul>
          <% sponsor.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="field">
      <%= form.label :company_name, "Sponsor Company Name" %>
      <%= form.text_field :company_name, placeholder: "Enter company name" %>
    </div>

    <div class="field">
      <%= form.label :contact_email, "Contact Email" %>
      <%= form.email_field :contact_email, placeholder: "contact@company.com" %>
    </div>

    <div class="field">
      <%= form.label :phone_number, "Phone Number" %>
      <%= form.telephone_field :phone_number, placeholder: "(555) 123-4567" %>
    </div>

    <div class="field">
      <%= form.label :company_description, "Company Description" %>
      <%= form.text_area :company_description, rows: 4, placeholder: "Tell us about your company..." %>
    </div>

    <div class="field">
      <%= form.label :website, "Sponsor Website" %>
      <%= form.url_field :website, placeholder: "https://www.company.com" %>
    </div>

    <!-- Logo Upload Section for Sponsor Users -->
    <div class="field" style="margin-top: 20px; padding: 15px; border: 1px solid #ddd; border-radius: 4px; background-color: #f9f9f9;">
      <%= form.label :logo, "Company Logo" %>
      
      <% if sponsor.logo.attached? %>
        <div style="margin: 10px 0;">
          <p style="font-weight: 500; color: #333;">Current Logo:</p>
          <%= image_tag sponsor.logo, style: 'max-width: 200px; max-height: 200px; border: 1px solid #ddd; border-radius: 4px;' %>
        </div>
      <% end %>
      
      <div style="margin-top: 10px;">
        <%= form.file_field :logo, accept: 'image/png,image/jpeg,image/jpg,image/gif,image/webp', style: 'margin-top: 5px;' %>
        <p style="font-size: 13px; color: #666; margin-top: 5px;">
          Accepted formats: JPEG, PNG, GIF, WebP (Max size: 5MB)
        </p>
        <% if sponsor.logo.attached? %>
          <p style="font-size: 13px; color: #666;">
            <em>Upload a new file to replace the current logo</em>
          </p>
        <% end %>
      </div>
    </div>

    <div class="form-actions">
      <%= form.submit "Save Changes", class: "btn-primary" %>
      <%= link_to "Cancel", sponsor_dashboard_index_path, class: "btn-secondary" %>
    </div>
  <% end %>
</div>