<% content_for :title, "Logo placement" %>

<p style="color: green"><%= notice %></p>

<h1>Logo Placement Details</h1>

<div>
  <p><strong>Page Name:</strong> <%= @logo_placement.page_name %></p>
  <p><strong>Section:</strong> <%= @logo_placement.section %></p>
  <p><strong>Displayed:</strong> <%= @logo_placement.displayed? ? "Yes" : "No" %></p>
</div>

<div>
  <%= link_to "Edit this logo placement", edit_admin_panel_sponsor_logo_placement_path(@sponsor, @logo_placement) %>
  <%= link_to "Back to sponsor", admin_panel_sponsor_path(@sponsor) %>
</div>