<% content_for :title, "Edit User Role" %>

<div class="user-edit-page">
  <div class="page-header">
    <h1>Edit User Role</h1>
    <p class="page-subtitle">Manage user access permissions</p>
  </div>

  <div class="form-container">
    <%= render "form", user: @user %>
  </div>

  <div class="page-actions">
    <%= link_to "← Back to Profile", @user, class: "btn btn-outline" %>
    <%= link_to "All Users", users_path, class: "btn btn-outline" %>
  </div>
</div>
